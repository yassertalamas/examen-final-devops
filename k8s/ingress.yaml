apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hola-mundo-ingress
  annotations:
    ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: hola-mundo-service
            port:
              number: 80
